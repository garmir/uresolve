name: Unique Test - uresolve
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - uresolve
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: uresolve ===" > unique-test-uresolve.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-uresolve.txt
            echo "Test ID: TEST_151" >> unique-test-uresolve.txt
            echo "Timestamp: $(date)" >> unique-test-uresolve.txt
            curl -s ifconfig.me >> unique-test-uresolve.txt
            echo "Unique instruction: General framework compliance and performance testing" >> unique-test-uresolve.txt
          '

      - name: Ubuntu Fallback - uresolve
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for uresolve" > fallback-uresolve.txt
          curl -s ifconfig.me >> fallback-uresolve.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-uresolve
          path: "*.txt"
          retention-days: 2